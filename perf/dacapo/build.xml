<?xml version="1.0"?>

<!--
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#      https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
-->

<project name="Dacapo suite" default="build" basedir=".">
	<taskdef resource="net/sf/antcontrib/antlib.xml" />
	<description>Dacapo Suite</description>

	<!-- Set global properties for this build -->
	<property name="DEST" value="${BUILD_ROOT}/perf/dacapo" />
	<property name="SRC" location="." />
		
	<target name="init">
		<mkdir dir="${DEST}" />
	</target>

	<target name="getDacapoSuite" depends="init">
		<var name="FILE_NAME" value="dacapo.jar"/>
    <var name="DIR_NAME" value="dacapo"/>
    <var name="DACAPO_DIR_NAME" value="dacapo-23.11-MR2-chopin-minimal"/>
		<var name="ZIP_FILE_NAME" value="${DACAPO_DIR_NAME}.zip"/>
		<var name="JAR_FILE_NAME" value="${DACAPO_DIR_NAME}.jar"/>
		<if>
			<available file="${FILE_NAME}" type="file" />
			<then>
				<echo message="${FILE_NAME} exists. Hence, not downloading it." />
			</then>
			<else>
				<var name="curl_options" value="-Lks -C - https://download.dacapobench.org/chopin/${DACAPO_DIR_NAME}.zip -o ${ZIP_FILE_NAME}"/>
				<retry retrycount="3">
					<sequential>
						<echo message="curl ${curl_options}" />
						<exec executable="curl" failonerror="true">
							<arg line="${curl_options}" />
						</exec>
					</sequential>
				</retry>
				<!-- Extract the entire ZIP file directly to source directory -->
				<echo message="Extracting all contents from ${ZIP_FILE_NAME} to ${SRC}" />
				<unzip src="${ZIP_FILE_NAME}" dest="${SRC}"/>

				<!-- Rename the main JAR to the expected name -->
				<echo message="Renaming ${JAR_FILE_NAME} to ${FILE_NAME}" />
				<move file="${SRC}/${JAR_FILE_NAME}" tofile="${SRC}/${FILE_NAME}" failonerror="true"/>

				<!-- Rename the directory from DACAPO_DIR_NAME to DIR_NAME -->
				<echo message="Renaming directory ${DACAPO_DIR_NAME} to ${DIR_NAME}" />
				<move todir="${SRC}/${DIR_NAME}" failonerror="false">
					<fileset dir="${SRC}/${DACAPO_DIR_NAME}"/>
				</move>
        
				<delete dir="${SRC}/${DACAPO_DIR_NAME}"/>
				<delete file="${ZIP_FILE_NAME}"/>
			</else>
		</if>
	</target>

	<target name="dist" depends="getDacapoSuite" description="generate the distribution">
		<copy todir="${DEST}">
			<fileset dir="${SRC}"/>
		</copy>
	</target>

	<target name="build">
		<antcall target="dist" inheritall="true" />
	</target>
</project>
