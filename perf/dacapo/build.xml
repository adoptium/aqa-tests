<?xml version="1.0"?>

<!--
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#      https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
-->

<project name="Dacapo suite" default="build" basedir=".">
	<taskdef resource="net/sf/antcontrib/antlib.xml" />
	<description>Dacapo Suite</description>

	<!-- Set global properties for this build -->
	<property name="DEST" value="${BUILD_ROOT}/perf/dacapo" />
	<property name="SRC" location="." />
		
	<target name="init">
		<mkdir dir="${DEST}" />
	</target>

	<target name="getDacapoSuite" depends="init">
		<var name="ZIP_FILE_NAME" value="dacapo.zip"/>
		<var name="JAR_FILE_NAME" value="dacapo.jar"/>
		<if>
			<available file="${JAR_FILE_NAME}" type="file" />
			<then>
				<echo message="${JAR_FILE_NAME} exists. Hence, not downloading it." />
			</then>
			<else>
				<var name="curl_options" value="-Lks -C - https://download.dacapobench.org/chopin/dacapo-23.11-MR2-chopin.zip -o ${ZIP_FILE_NAME}"/>
				<retry retrycount="3">
					<sequential>
						<echo message="curl ${curl_options}" />
						<exec executable="curl" failonerror="true">
							<arg line="${curl_options}" />
						</exec>
					</sequential>
				</retry>

				<!-- extract zip file -->
        <property name = "tmp" value = "${SRC}/tmp"/>
        <mkdir dir="${tmp}"/>
        <unzip src="${ZIP_FILE_NAME}" dest="${tmp}">
            <patternset>
                <include name="**/*.jar"/>  
            </patternset>
        </unzip>

        <fileset id="jar.files" dir="${tmp}" includes="**/*.jar"/>
        <pathconvert property="extracted.jar" refid="jar.files"/>
        <move file="${extracted.jar}" tofile="${JAR_FILE_NAME}" failonerror="true"/>

        <delete dir="${tmp}"/>
        <delete file="${ZIP_FILE_NAME}"/>
			</else>
		</if>
	</target>
	
	<target name="dist" depends="getDacapoSuite" description="generate the distribution">
		<copy todir="${DEST}">
			<fileset dir="${SRC}"/>
		</copy>
	</target>

	<target name="build">
		<antcall target="dist" inheritall="true" />
	</target>
</project>
