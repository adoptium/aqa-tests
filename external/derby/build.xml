<?xml version="1.0"?>
<project name="Derby-Test" default="build" basedir=".">
	<taskdef resource="net/sf/antcontrib/antlib.xml" />
	<description>
		Build derby-test docker image
	</description>
	<import file="${TEST_ROOT}/external/build.xml"/>

	<!-- set properties for this build -->
	<property name="EXT_DEST" value="${BUILD_ROOT}/external" />
	<property name="DEST" value="${BUILD_ROOT}/external/derby" />
	<property name="dist" location="${DEST}/${JDK_VERSION}" />
	<property name="src" location="." />
	<property name="top" location="../" />

	<!-- properties to generate Dockerfiles -->
	<property name="TEST" value="derby" />
	<property name="VERSION" value="${JDK_VERSION}" />
	<property name="VM" value="${env.JDK_IMPL}" />
	<property name="OS" value="${env.DOCKERIMAGE_TAG}" />
	<property name="PACKAGE" value="jdk" />
	<property name="BUILD" value="full" />

	<target name="init">
		<mkdir dir="${DEST}"/>
	</target>

	<target name="clean_image" depends="init" description="clean derby test docker image if there is one">
		<exec executable="docker">
			<arg line="rmi -f adoptopenjdk-derby-test" />
		</exec>
	</target>

	<target name="move_scripts" depends="clean_image" description="">
	<echo message=" external/derby move scripts ........ ${top}"/>
		<copy todir="${EXT_DEST}">
			<fileset dir="${top}" includes="*.sh"/>
		</copy>
	</target>

	<target name="build_image" depends="move_scripts" description="build derby test dockerfile">
		<echo message="Generate dockerfile!!! : build_image.sh ${TEST} ${VERSION} ${VM} ${OS} ${PACKAGE} ${BUILD}" />						
		<exec executable="bash">
			<arg value="${EXT_DEST}/build_image.sh"/>
			<arg value="${TEST}"/>
			<arg value="${VERSION}"/>
			<arg value="${VM}"/>
			<arg value="${OS}"/>
			<arg value="${PACKAGE}"/>
			<arg value="${BUILD}"/>
		</exec>
	</target>

	<target name="dist" depends="build_image" description="generate the distribution">
		<copy todir="${DEST}">
			<fileset dir="${src}" includes="*.xml, *.mk"/>
		</copy>
	</target>

	<target name="build">
		<antcall target="dist" inheritall="true" />
	</target>
</project>
