[
    {
        "COMMENT": "JDK 11 and earlier specific JCK configuration overrides. For JDK 8, 11 - Windows does NOT need -Djava.awt.headless=false (only JDK 24+ needs it). AIX uses sw.os.aix.7_2. x86-64_linux does NOT use glibc exclusion (only JDK 17+ needs it).",
        "PLATFORM_SPECIFIC_CONFIG": {
            "jck": {
                "x86-64_windows": {
                    "ADDITIONAL_TEST_PARAMS": {
                        "EXTRA_OPTIONS": " -Xmx512m"
                    }
                },
                "aarch64_windows": {
                    "ADDITIONAL_TEST_PARAMS": {
                        "EXTRA_OPTIONS": " -Xmx512m"
                    }
                }
            }
        },
        "PLATFORM_ADDITIONAL_TEST_LABELS": {
            "x86-64_linux": "",
            "ppc64_aix": "sw.os.aix.7_2"
        }
    }
]